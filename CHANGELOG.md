# Changelog

## v2.0.0 (build 17) — 2026-08-14 · 실기기 실측 + adb 정밀 측정 반영

v2.0 실기기 체크리스트 실측(2026-08-14)과 갤럭시 순정 모아키 **adb 터치 주입
정밀 측정**(사람 손 오차 없이 각도·길이를 픽셀 단위 통제)을 반영했다.

### 입력 엔진
- **손 떼는 꼬리 오타 완화 (a5·b2·b6·T1)** — 되돌림 등록 하한을 키폭 20% → **28%**
  로 상향 (`reversalThresholdRatio` 0.70, 구 저장값 자동 승격). 순정 하한을 adb
  주입으로 41px 미등록/42px 등록(키 너비 150px의 28%)으로 확정했고, 진입 길이
  무관(절대 임계)임을 재확증. ㅚ→ㅛ, ㅕ→ㅖ("녕"→"녱") 승격 오타의 원인이던
  10~14pt 대역 꼬리가 걸러진다. 방향 판정 window 는 등록 임계와 분리해 키폭
  20% 유지 (의도적 되돌림 과소인식 방지).
- **ㆍ 무한 토글 확장 (a6)** — 애+ㆍ=얘, 얘+ㆍ=애, 에+ㆍ=예, 예+ㆍ=에
  (순정 adb 실측 확증 — ㅗ↔ㅛ/ㅜ↔ㅠ와 같은 규칙).
- **마지막-성분 ㆍ 결합** — 외+ㆍ=와(+ㅣ=왜), 위+ㆍ=워(+ㅣ=웨) (천지인 표준,
  순정 adb 실측 확증). ㅢ+ㆍ는 순정이 옛한글 ㅡㅏ를 만드는 것을 확인 — 현대
  한글 밖이라 재현하지 않고 의 커밋 + ㆍ 대기 유지.
- **세로 체인 모서리 튕김 흡수** — ↑↓ 긋고 →/←로 꺾는 모서리의 작은 위쪽
  들림이 ↑↓↑→로 등록돼 ㅛ가 ㅘ/ㅕ를 가로채던 잔여 오타(build 17 실기기 재검증)
  수정. 양옆 획의 50% 미만인 중간 튕김을 다음 획에 벡터째 흡수 (키폭 비례 상한).
- 확인만 하고 변경 없음: ㅓ/ㅣ 경계 22.5°(실측 22°/23° 사이), 첫 획 4방향
  재해석 45° 옥탄트(55~65° 정밀 입력 = 수직), ↘↖ 반전 = ㅢ 우선, ↘/↓ 경계
  65~70°(R4 브래킷 정합).

### 앱
- **입력 기록 보드 신설** (설정 하단) — 실측·디버그용 자유 입력 보드, 오타 사례
  저장/복사/공유. 기기 로컬에만 저장. **개발자에게 보내기** 지원: 기록 + 긋기
  설정 요약 + 앱·기기 정보를 메일 또는 GitHub 이슈로 전송 (전송 전 내용 확인 안내).
  **제스처 상세 기록**(기본 ON): 긋기마다 획별 방향·크기·각도(트림 전/후)와 결과를
  기기 로컬에 순환 기록(200건)하고 리포트에 최근 80건 동봉 — 전송은 사용자가
  리포트 보내기를 눌러야만 일어난다.
- **긋기 테스트에 획별 측정 카드** — 획마다 방향·각도(°)·길이(pt/키폭%)를 표시 (T2).
- 백스페이스 설정: "한 자소씩" 안내를 상시 노출 (기존엔 단어 삭제 OFF일 때만 보였음).
- 긋기 설정: '복합모음 입력' 피커 라벨이 선택지처럼 오인되던 문제 수정 (섹션 헤더로 이동).
- 설정 검색: "자소" 검색이 백스페이스 항목으로 이어지도록 제목·키워드 보강.

## v2.0.0 (build 16) — 순정 모아키 실측 정합화

갤럭시 순정 모아키의 실제 입력을 녹화한 관찰 영상 47편(기본 40 + 재촬영 7)을 프레임
단위로 판독해 입력 규칙을 실측으로 확정하고, 우리 엔진과 다른 지점을 전부 정합화했다.
판독 전문은 `docs/MOAKEY_VIDEO_FINDINGS.md`, 남은 미확정 항목은 `docs/MOAKEY_RESHOOT_LIST.md`.

### 입력 엔진 (순정 실측 반영)
- **대각선 왕복 모음** — 자음 키에서 ↗로 나갔다 ↙로 되돌아오면 ㅐ, ↖↘는 ㅔ, ↙↗는 ㅢ.
  리뷰 "ㅐ 입력 방향이 순정과 다르다"(호떡애비)의 원인이 이것이었다(순정 사용자 ㅐ 입력의
  2/3가 이 경로, 영상 C 섹션). 구현은 순정의 판정 모델 그대로: 첫 대각선 획을 8방향으로
  잠정 두고, 후속 획이 등록되면 실제 각도의 4방향 재해석과 트라이 매칭을 겨뤄 더 많은
  간선을 따라간 쪽을 채택(동률 = 기존 해석 → ↙↗=ㅢ 보존). 손 떼는 꼬리의 오발동은
  두 번째 획이 키 폭 0.6배 이상일 때만 재해석을 발동해 차단.
- **세로 체인 확장** — ㅚ(↑↓)에서 이어 그으면 →=ㅘ, →←=ㅙ, ←=ㅕ, ←→=ㅖ, ↑↓=ㅠ.
  ㅟ(↓↑)에서 ←=ㅝ, ←→=ㅞ. 순정 미리보기 팝업 체인(고→괴→과→괘 등)으로 확정된 간선들.
- **수평 획 뒤 직각 꺾기** — →긋고 ↑/↓로 꺾으면 ㅐ, ←긋고 꺾으면 ㅔ (순정은 수평 시작
  트라이에 직각 간선이 없어 반전으로 스냅함을 재촬영 R2로 확정). 수직 시작(↑→=ㅘ 등)은
  기존대로.
- **ㆍ 토글** — ㅛ+ㆍ=ㅗ, ㅠ+ㆍ=ㅜ 무한 토글 (순정 H2 실측, 스펙에 없던 규칙).
- **자소 단위 백스페이스** — 받침 → 모음(통째) → 자음 순으로 지운다. 받침 없는 완성
  글자에서 초성을 남긴다(가→ㄱ, 기존은 글자 전체 삭제). 순정 G5 실측과 동일.
- 순정과 이미 일치함을 확인만 한 것(변경 없음): 단독 대각선 ↗↖=ㅣ/↙↘=ㅡ(17회 예외 없음),
  45° 등분 섹터(5개 경계 실측 정합), 복합모음 12종, 받침 결합·이월·겹받침 분해(값+ㅏ=갑사),
  `consonantDiagonalDerivationEnabled` 기본 OFF 유지.

### 앱 (튜토리얼·연습·설정 문구)
- 튜토리얼 3단계를 "대각선 왕복 — ㅐ·ㅔ·ㅢ"로 재구성 (구 대각선 진입 파생 단계는 기본
  OFF 기능을 가르치고 있었다). 6단계 ㅞ 방향 표기 오류(↓→← → ↓←→) 수정, ㅢ 대체 경로
  (↙↗) 병기, 자소 단위 백스페이스 안내 추가.
- 타이핑 연습 34번을 "대각선 왕복 모음 (ㅐ·ㅔ·ㅢ)"으로 교체.
- 긋기 설정의 순정 모드 설명에 왕복 경로 추가, 백스페이스 설정 문구를 자소 단위로 정정,
  설정 검색 키워드 보강("ㅐ", "애", "자소", "받침" 등).
- 확장형 모음 키 라벨을 순정과 같은 "ㅣㆍㅡ / 모음" 표기로 변경.

### 테스트
- `MoakeyVideoVerifiedSpecTests` (신규 30+ 케이스) — 영상 근거를 주석으로 단 순정 스펙
  가드. 트라이 확장·재해석·ㆍ 토글·자소 백스페이스·ㅚ 꼬리 회귀 가드 포함.
- 기존 백스페이스 테스트 2건을 순정 실측값으로 갱신. 특성화 테스트('으'→'워' 오타 가드)
  전부 유지 통과.

## Unreleased — 입력 반응속도 + 설정 발견성

### 새 기능
- **설정 검색** — 설정 첫 화면에서 원하는 항목을 검색. 앱 내부 용어를 몰라도 되도록 **사용자가 실제로 쓰는 말**을 검색어로 받는다: "진입앵글"·"각도"로 긋기 각도가, "지구본"(오타 표기 "지구봉" 포함)으로 키보드 전환 키가, "작은 글자"로 키 힌트가 나온다. 결과에 탐색 경로(설정 › 키보드)를 함께 보여줘 다음부터는 직접 찾아갈 수 있다.
- **"이럴 때 어떻게 하나요"** — 증상으로 설정을 찾는 화면(구 도움말). "오타가 잦아요", "빨리 치면 ㅛ·ㅠ·ㅢ 가 안 나와요", "키에 작은 글자가 보여요"처럼 사용자 말로 8개 항목을 세우고 해당 설정으로 바로 보낸다. 튜토리얼·타이핑 연습은 그대로 아래에 있다.
  - 배경: 앱스토어 리뷰 7건 중 5건이 "기능이 없다"가 아니라 **있는 기능을 찾지 못한** 경우였다. 설정이 11개 화면으로 늘면서 기능의 앱 내부 명칭(멀티스트로크, 섹터 각도, 보조 매핑)을 모르면 도달할 길이 사실상 없었다.

### 변경
- 설정의 **"반응" → "소리 · 진동"** 으로 개명. 내용은 사운드·햅틱뿐인데 이름이 "반응"이라, "반응속도가 느리다"고 느낀 사용자를 끌어들여 빈손으로 돌려보내고 있었다.
- 설정 항목 위치·저장값은 그대로다. 기존에 쓰던 화면을 다시 찾아야 하는 변경은 없다.

### 성능 (입력 반응속도)
긋기 중 화면을 다시 그리는 횟수와 매 터치 포인트마다 반복되던 계산을 줄였다. 리뷰 "반응속도도 조금 느린것같긴해요"(돌양파) 대응.
- **긋기 한 번에 키보드 전체가 스무 번 넘게 다시 그려지고 있었다.** 터치 포인트마다 제스처 상태를 값 비교 없이 다시 대입해, 값이 그대로여도 SwiftUI 가 전체 트리를 재평가했다. 실제로 값이 바뀌는 건 한 번의 긋기에서 2~4회뿐이다.
- 활성 키가 자음인지 모음키인지 판정하려고 터치 포인트마다 키 배열 전체(4×7)를 새로 만들고 있었다. 제스처 시작 시 한 번만 조회하도록 바꿨다.
- 키보드를 한 번 그릴 때 레이아웃을 6번 재구축하던 것을 1번으로, 롱프레스 매핑 조회를 29개 배열 선형 탐색에서 딕셔너리로, 키 색상을 키마다 새로 계산하던 것을 테마가 바뀔 때만 계산하도록.
- 설정 하나가 바뀌면 무관한 설정까지 전부 다시 발행돼 키보드가 통째로 재구성되던 것을 값이 실제로 달라진 항목만 반영하도록 수정.
- 쓰이지 않는 햅틱 객체를 콜드스타트마다 만들고 Taptic Engine 을 켜둔 채 대기하던 코드 제거.
- **아직 실측은 하지 않았다.** 위 항목들은 코드상 명백한 낭비를 제거한 것이고, 체감 속도가 얼마나 좋아지는지는 실기기 프로파일링으로 확인해야 한다. 입력 인식 동작은 바뀌지 않았다(긋기 특성화 테스트로 확인).

### 테스트
- `KeyboardSettingsCacheTests` (신규) — 색상·롱프레스 인덱스 캐시가 원본 변경을 항상 따라오는지. 스냅샷 테스트는 정적 렌더 1회만 보므로 런타임 설정 변경 경로를 덮지 못한다. 캐시 정확성이 기대는 전제(`ThemeSettings` 의 합성 `Equatable` 이 색상 채널까지 비교)도 채널별로 못박았다.
- `SettingsDiscoveryUITests` (신규, CI 미편입) — 시뮬레이터에서 검색·증상 라우터·화면 제목 일치를 확인. `MoaPlusUITests` 타겟이 스킴에서 제외돼 있고 `TEST_TARGET_NAME` 이 포크 이전 이름을 가리켜, 실행하려면 우회가 필요하다(실행법은 파일 헤더 주석 참조).

---

## Unreleased — 순정 모아키 입력 방식 + 긋기 오인식 수정

### 입력 방식
- **복합모음 입력이 기본적으로 순정 모아키 방식이 됐다** — 설정 → 키보드 → 제스처 → 입력 방식에서 `순정 모아키`(기본) / `확장 (대각선 진입)` 선택. v1.7에 추가했던 "자음을 대각선으로 그은 뒤 이어 그어 천지인 파생"(`resolveConsonantDiagonalVowel`)은 **순정에 없는 확장 경로**였고, 이제 기본 OFF다.
  - 순정 스펙 확인: 저장소의 실제 삼성 모아키 설정 스크린샷(`docs/moakey_ios_custom_docs/assets/03_gesture_angle_reference.png`, 양손용 — ↑=오 ↓=우 →=아 ←=어 ↖↗=이 ↙↘=으)과 [나무위키 모아키](https://namu.wiki/w/모아키) 문서가 일치. 복합모음은 방향 **조합**으로 만든다: ㅘ=↑→(↱), ㅝ=↓←(↲), ㅚ=↑↓, ㅟ=↓↑, ㅐ=→←, ㅔ=←→, ㅛ=↑↓↑, ㅠ=↓↑↓, ㅑ=→←→, ㅕ=←→←, ㅙ=↑→+왕복, ㅞ=↓←+왕복.
  - 대조 결과 **자음 키 패턴 테이블(`VowelPattern.all`)은 이미 순정과 동일**했다. 불일치는 대각선 진입 파생 하나뿐이었다.
  - 단독 대각선(↗=ㅣ, ↘=ㅡ)은 순정과 같으므로 그대로다 — 전용 ㅣ/ㅡ 키가 없는 클래식/확장형 레이아웃의 유일한 ㅣ/ㅡ 입력 경로라 회귀 가드를 뒀다.

### 버그 수정 (긋기 오인식)
- **긋기 끝의 흔들림이 모음을 바꾸던 문제** — 리뷰 3건(몰라디 3★ "아·오가 이·와로", 므미므므 4★ "'이'가 '와'로, '으'가 '워'로", 콩픈패스 "'와'·'오'가 잘 안 써짐")이 같은 원인이었고, 코드에서 재현한 뒤 수정했다. 손을 떼며 튕기는 15~20pt 꼬리가 새 획으로 등록돼 ㅡ 를 ㅗ/ㅢ/ㅘ 로 승격시키고 있었다.
  - `trimTinyLeadingAndTrailingNoise` 의 후행 판정이 **인접(≤45°) 꼬리만** 제거하도록 돼 있었다. 실제 꼬리는 ↗(180°)·↑(135°)·↘(90°)처럼 방향이 급격한 쪽이 더 흔해 전부 빠져나갔다 — 의도와 정반대였다. 이제 절대 크기와 **직전 획 대비 비율**을 함께 보고, 꼬리가 여러 조각으로 등록된 경우를 위해 반복 제거한다.
  - **`directionMagnitudes` 가 획의 실제 길이가 아니었다** — 임계를 넘는 순간의 변위만 기록해, 60pt 를 그은 획도 임계값(≈20pt)으로 남았다. `normalizeSegments` 의 비율 판정이 전부 이 값을 "직전 획 길이"로 전제하고 있었으므로 노이즈와 의도적 획을 비율로 구분할 수 없었다. 획이 이어지는 동안 실제 길이로 갱신하도록 고쳤다(`strokeOriginPoint`).

### 테스트
- `GestureOverDetectionCharacterizationTests` (신규) — 다구간 폴리라인 하니스 `drivePath`(기존 `driveKeyGesture` 는 단일 직선만 가능해 꺾인 궤적을 재현할 수 없었다) + 경로 × 민감도(0/1/2) 매트릭스를 `.xcresult` 에 첨부. 리뷰의 오타 4갈래(후행 과등록 / 진입 분열 / 과소 인식 / 매핑 차이)를 한 표에서 관찰한다.
- 회귀 가드: 의도적 복합모음 ㅒ(→←→←)·ㅖ·ㅙ·ㅞ 의 짧은 마지막 획(30pt)이 트림에 깎이지 않는지, 빠른 입력(샘플 2점)에서 ㅛ/ㅠ 가 여전히 인식되는지, 기울인 ↓↑ 가 ㅟ 를 내는지(똑데 리포트). 후행 트림을 절대 크기만으로 강화했을 때 실제로 얘→야, 왜→와 회귀가 발생해 이 가드가 잡아냈다.

### 알려진 잔여
- 기본 민감도(0)에서 ↙ 뒤 15~20pt 흔들림이 아직 ㅢ 로 승격된다. ㅢ 는 순정에도 있는 조합(↙↗)이라 논리적으로 도달 가능한 값이고, '와/워'와 달리 경미하다. 더 좁히려면 등록 임계를 건드려야 해 과소 인식(리뷰 "빨리 치면 인식 안 됨" 갈래) 위험이 생겨 여기서 멈췄다. 테스트로 고정해 뒀다.
- 진입 획이 짧을 때(30pt)는 꼬리(18pt)가 그 61%라 비율로도 노이즈 판정이 불가능하다 — 의도적인 짧은 왕복(ㅢ 자체)과 구분되지 않아 한계로 남긴다.

---

## Unreleased — 앱스토어 리뷰 반영 (높이 조절 + 키보드 전환 키)

### 새 기능
- **키보드 높이 조절** — 설정 → 키보드 → 크기 · 전환 키에서 키보드 높이를 85%~135%로 조절. 기기별 기본 높이(아이폰 260pt / 아이패드 화면 실측)에 배율이 곱해지며, 아이패드도 같은 배율이 적용된다. 설정 화면의 실시간 미리보기가 배율을 그대로 반영하고, "기본값으로 되돌리기" 버튼으로 100%로 복원. 하한 85%는 키 한 행이 38pt 아래로 내려가 오타가 늘지 않도록 `KeyboardMetrics`에서 클램프한다 (저장값이 손상돼 NaN/범위 밖이어도 동일하게 방어). 리뷰 4건(미로아입니다·나가방·Geumji5·콩픈패스) 요청.
- **키보드 전환 (지구본) 키** — 설정 → 키보드 → 크기 · 전환 키에서 켜면(**기본 OFF**) 기능 행 맨 왼쪽에 지구본 키가 생겨 애플 기본 키보드 등 다른 키보드로 바로 전환. 이전에는 `KeyboardViewModel.switchKeyboard()`가 어떤 뷰에서도 호출되지 않아 모아+로 들어오면 다른 키보드로 나갈 방법이 없었다 (아이패드 분리 레이아웃의 지구본도 동작 없음). 켜면 기능 행에 키가 하나 늘어 **스페이스바가 그만큼 좁아진다**(다른 키 폭·위치는 그대로) — 업데이트만으로 레이아웃이 바뀌지 않도록, 또 아래 iOS 26 항목대로 최신 아이폰에는 시스템 지구본 바가 이미 있으므로 기본값을 OFF 로 뒀다. 리뷰 2건(이게뭐시요·뚜두뚜3) 요청.

### 변경
- **⚠️ iOS 26 아이폰에서는 지구본 키가 표시되지 않는다 (시뮬레이터 실측)** — iOS 26 은 서드파티 키보드 **아래에 지구본+받아쓰기 시스템 바를 직접 그려주며**, 이때 `needsInputModeSwitchKey` 가 `false` 를 반환한다. 우리 지구본은 이 값에 게이팅돼 있어(중복 지구본 방지) 렌더되지 않는다. 즉 **iOS 26 아이폰에서 이번 변경은 사실상 무동작**이고, 시스템 바가 없어 `needsInputModeSwitchKey == true` 인 환경에서만 기능 행에 나타난다. iPhone 17 Pro / iOS 26.5 시뮬레이터 확인: 모아+ 활성화 시 우리 지구본 미표시, 시스템 바 지구본으로 모아+ ↔ 영문 전환은 정상. 리뷰어 2인이 어떤 iOS 버전이었는지 불명이므로, 게이팅을 유지할지(현재) 설정만으로 항상 표시할지는 재검토 여지가 있다.
- **새로운 기능 안내 모달** — 위 두 기능과, 리뷰에서 "요청"으로 올라왔지만 이미 존재하던 각도 조절 설정의 위치 안내로 교체.

### 버그 수정 (키보드 멈춤 리포트 대응)
- **배경 이미지가 원본 해상도로 디코딩되던 문제** — 사용자가 고른 사진을 축소 없이 저장하고, 키보드 익스텐션에서 `UIImage(contentsOfFile:)` 로 전량 디코딩하고 있었다. 12MP 사진(4032x3024)은 비압축 ARGB 로 약 48MB — 익스텐션 메모리 한계(~30-60MB)를 **단독으로** 넘긴다. 한계를 넘으면 iOS가 익스텐션을 종료하고, 사용자에게는 "키보드가 멈춤"으로 보인다. 저장 시 긴 변 1536px 로 축소하고, 불러올 때도 ImageIO 썸네일 디코딩으로 전체 비트맵을 아예 만들지 않도록 바꿨다. 축소 도입 이전에 저장된 원본 파일도 불러오기 경로에서 방어된다.
- **키보드가 사라져도 백스페이스 반복 타이머가 계속 돌던 문제** — 백스페이스를 누른 채로 호스트 앱이 사진 선택기 등을 띄우면 키보드가 내려가는데, 반복은 터치가 아니라 Timer 로 구동돼 아무도 멈추지 않았다. 보이지 않는 필드에 `deleteBackward()` 가 계속 나갔고, 정리는 *다음* `viewDidAppear` 에서야 이뤄졌다. `viewWillDisappear` 에서 정리하도록 수정.
- 두 건 모두 "제미나이에서 사진 첨부 시 키보드가 멈춘다"(Geumji5, iPhone 15 Pro) 리포트를 조사하다 발견한 것으로, **그 리포트의 확정 원인은 아니다** — 재현이 확인되지 않았다. 함께 보고된 "키보드 높이가 엄청 늘어남"은 [KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md) KI-1(iOS 시스템 입력 컨테이너 frame 누적, 익스텐션 코드로 해결 불가)과 같은 계열로 보이나 트리거가 달라 동일 이슈로 단정하지 않는다.

### 테스트
- `KeyboardMetricsLayoutTests` — 배율 기본값이 도입 이전과 동일(회귀 잠금), 아이폰/아이패드 배율 적용, 범위 밖·NaN 클램프, 하한에서도 키 높이 38pt 이상.
- `GlobeKeySwitchTests` (신규) — 지구본 탭이 델리게이트까지 도달, 전환 전 조합 확정(다른 키보드를 거쳐 와도 이전 글자가 되살아나지 않음), `canSwitchInputMode` 기본값.
- `BackgroundImageMemoryTests` (신규) — 12MP 축소, 작은 이미지 무손상, 포인트가 아닌 **픽셀** 기준 측정(scale 누락 시 3x 이미지가 3배로 저장됨), 디코딩 메모리 상한, 저장→로드 왕복.
- `KeyboardSnapshotTests` — 아이폰 실제 치수(402pt)로 지구본 ON/OFF + 높이 배율 하한/상한 렌더 스냅샷 추가. 시뮬레이터에서 익스텐션을 띄우려면 시스템 키보드 전환 UI 자동화가 필요해 불안정하므로, 프로덕션 뷰 렌더로 잘림·레이아웃을 검증한다.
- `FunctionRowWidthTests` (신규) — 기능 행 4개 바디(기본/심볼/긴 스페이스/양손) × 지구본 on/off 에서 자식 폭 합 == 사용 가능 폭. 자식을 추가하면 간격 수도 늘려야 하는데 이를 놓치면 ⏎ 키가 잘리므로 불변식으로 고정. 지구본이 스페이스바만 줄이고 다른 키 폭은 건드리지 않는 것도 검증.

---

## v1.8.0 (build 15) — 2026-07-20

### 새 기능
- **특수문자 키패드 2페이지화** — 숫자/특수문자 키패드를 두 페이지로 확장. 1페이지(숫자판)에 마침표·쉼표·따옴표(`. , ' "`)를 왼쪽 열에 추가하고, 2페이지(기호판)에 괄호·통화·수학·타이포 기호(`[ ] { } ₩ € £ ¥ · … © ®` 등)를 배치. 기능 행의 `#+= / 123` 전환 키(스페이스 왼쪽)로 페이지를 넘긴다. 마침표 키가 생기면서 소수점 입력(예: `2.000`) 시 더블 스페이스 우회로 공백이 끼던 불편도 자연 해소.
- **스페이스 드래그 연속 이동** — 스페이스바를 좌/우 끝 15% 구역(바 폭 비례, 작은 폰 대응)으로 밀고 유지하면 그 방향으로 커서가 연속 이동하며 점점 빨라진다. 드래그 중 양쪽 구역과 방향 화살표(`‹` `›`, 테마 글자색)가 표시되고 진입한 쪽이 강조된다. 연속 이동 속도는 설정에서 느림/보통/빠름으로 조절. (커서 상하 이동은 iOS 확장 API 제약으로 미지원)

### 테스트
- 심볼 2페이지 레이아웃/페이지 인지 탭/프리셋별 필수문자 잔존, 스페이스 연속 이동 리피터·속도 매핑 테스트 추가.

### 버전
- 1.7.2 → 1.8.0 (build 14 → 15).

---

## v1.7.2 (build 14) — 2026-07-16

### 버그 수정
- **커서 탭 이동 후 마지막 글자 중복 삽입** — 글자 조합 중(예: "가나다"의 `다`) 텍스트 필드의 다른 위치를 탭해 커서를 옮긴 뒤 입력하면 직전 조합 글자가 새 위치에 다시 삽입되던 문제("가나다" → 맨 앞 탭 → `라` 입력 시 "다라가나다")를 수정. 커서 탭 시 `selectionDidChange`가 발생하지 않고 필드 맨 앞에서는 before 컨텍스트가 nil이라 기존 감지 경로가 모두 침묵하던 것이 원인 — 입력 시점에 "커서가 조합 글자 바로 뒤"라는 확증이 없으면 조합을 동결하는 backstop을 추가. before/after 컨텍스트를 모두 못 주는 호스트(시큐어 필드 등)에는 개입하지 않아 기본 조합을 보존.
- **단축어 확장 직후 자동 마침표 오발동** — 단축어가 확장되면(예: `ㅎㅌ`→`하트 `) 끝 공백이 함께 삽입되는데, 바로 다음 스페이스가 "첫 공백"으로 오인되어 더블 스페이스 마침표가 발동하던 문제(`하트 ` + 스페이스 → `하트. `)를 수정. 확장 직후 1회에 한해 마침표 단축을 억제하며, 글자 입력 후에는 정상 동작. 백스페이스 복원 → 재확장 사이클에서도 동일하게 억제.

### 테스트
- 회귀 테스트 2종 추가 (`KeyboardViewModelCaretMoveTests`, `KeyboardViewModelAbbreviationPeriodTests`) — 실기기 로그로 확인한 proxy 동작(맨 앞에서 before 컨텍스트 nil)을 mock에 반영.

### 버전
- 1.7 → 1.7.2 (build 12 → 14).

---

## v1.7 (build 12) — 2026-06-25

### 새 기능
- **iPad 정식 지원** — 아이패드 universal 빌드. 가로에서 좌우 분리 키보드(숫자패드 + 모아키 그리드), 세로 분리 토글, 화면 크기에 맞춘 동적 높이를 지원. (이전엔 iPhone 호환 모드로만 동작)
- **자음 대각선 진입 모음** — 자음 키를 ㅣ/ㅡ 방향(↗↖ / ↙↘)으로 긋고 이어서 그으면 모음이 완성됨. 예: ㄱ을 ↗→ = `가`, ↙↑ = `고`. 천지인 정통 규칙으로 21개 모음 전부 파생.
- **새로운 기능 안내 모달** — 업데이트 후 첫 실행 시 주요 변경을 카드로 1회 안내. (신규 사용자는 레이아웃 선택 모달만 표시)

### 제스처 / 입력 정확도
- **8방향 좌·우 각도 세밀 설정** — 각 방향 섹터의 좌/우 폭을 따로 조정(per-side). 넓히면 인접 방향에서 가져오고, 좁혀 생긴 빈 각도는 가장 가까운 방향으로 인식. "빈 각도 자동 인식" 토글로 끌 수 있음.
- **4방향 전용 모드** — 대각선을 끄고 상하좌우만 각 90°로 인식. "ㅗ·ㅜ 각도를 넓혀도 대각선이 먼저 먹던" 문제를 근본 해결.
- **멀티스트로크 민감도** — ㅛ·ㅑ 등 왕복 모음을 원점 복귀 없이 큰 각도 전환만으로 인식하는 민감도(끔/보통/민감) + 진폭 가드로 떨림 오인식 완화.
- **다단계 모음 인식 개선** — ㅛ·ㅠ 등을 여러 번 꺾어 입력할 때의 중간 오류를 허용해 인식률 향상.
- **후속 획 인식 정확도 개선** — 긴 진입 획(대각선 등) 뒤의 방향 전환을 최근 궤적 기준으로 판정. 후속 획이 진입 방향에 흡수되거나 전환 중간에 엉뚱한 방향이 끼던 문제를 수정.

### UX 개선
- **긋기 테스트 시각화** — "방향 전환 인식 범위"(민감도 반영) 원과 실시간 인식 결과를 캔버스에 표시. 긋는 중에도 자음 대각선 파생 모음을 미리 보여줌.
- **튜토리얼·자판 연습 보강** — 자음 대각선 입력 안내(튜토리얼 2단계)와 연습 항목(자판 연습) 추가.

### 버전
- 1.6 → 1.7 (build 11 → 12). iPhone 전용 → iPhone / iPad universal.

---

## v1.6 (build 11) — 2026-05-21

### 새 기능
- **더블 스페이스로 마침표 입력** — 글자 뒤에서 스페이스를 두 번 누르면 마침표와 공백(`. `)으로 자동 변환. iOS 기본 키보드와 동일한 동작이며, 설정 → 입력 동작에서 끌 수 있음 (기본 ON). 구두점·줄 시작·공백 뒤에서는 발동하지 않음.

### 버그 수정
- **단축어 변환 후 백스페이스 오류** — 단축어가 확장된 직후(예: `ㅎㅌ`→`♥`) 백스페이스를 누르면 변환 결과가 지워지지 않고 원본 약어가 뒤에 다시 붙던 문제(`♥ㅎㅌ`)를 수정. 약어 확장이 삽입한 구분자(공백)까지 복원 계산에 포함하도록 고쳐, 백스페이스 1회로 원래 입력으로 깔끔하게 복원됨.

---

## v1.5 (build 9) — 2026-05-15

### 새 기능
- **앱 내 메일 문의** — 앱 정보 → 이메일 문의가 앱 내 메일 작성기로 동작. 앱 버전 / iOS 버전 / 기기 모델 / 접근 권한이 본문에 자동 첨부되어 버그 보고가 진단 정보와 함께 도착. 메일 미설정 기기는 안내 후 신고 폼으로 안내.
- **심볼 모드 롱프레스 대체 문자** — 심볼 키패드에서 길게 누르면 대체 기호: `$`→₩ € £ ¥ / `*`→※ ★ ☆ / `(` `)`→[ ] { } < > / `-`→– — • / `/`→\\ / `:`→;
- **우측 컬럼 특수키 슬롯 독립화** — 모던 `#` 자리·확장형 1번 셀의 특수키 슬롯이 스페이스 옆 특수키와 별개로 편집됨. 한쪽을 바꿔도 다른 쪽이 따라 바뀌지 않음.

### UX 개선
- **레이아웃 설정 용어 정리** — "긋기 펑크" → "특수키", "슬롯 A/B/C" → "우측 컬럼 / 좌측 컬럼 / 스페이스 옆 키". 셀에서 내부 row 번호 노출 제거, 빨강 "초기화" → "되돌리기".
- **확장형 우측 컬럼 1번 셀** — `#` 고정에서 사용자 편집 슬롯 그대로 반영 (클래식과 공유).
- **한글 펑크 OFF** — 라디오/슬롯 편집기 자동 숨김 + 백스페이스 폭 2셀 정렬로 우측 라인 유지.
- **'전체 접근 허용' 안내** — 햅틱 설정 하단에 권한 안내 + iOS 설정 딥링크. 진동이 안 울리는 이유를 앱 내에서 바로 안내.

### 버그 수정
- **콜드 스타트 깜빡임 제거** — 키보드 첫 등장 시 (1) 시스템 그레이→설정 색, (2) 기본→사용자 레이아웃, (3) hosting view reattach 3중 flicker 제거. 설정을 SwiftUI 호스팅 이전에 로드하고, 첫 등장에는 touch-recovery 토글을 스킵.
- **무조건 클릭음 제거** — `clickSoundEnabled`를 무시하고 키보드 표시 때마다 울리던 오디오 워밍업 클릭 제거.

### 제스처
- **긋기 길이 ↔ 반전 임계값 연동** — "긋기 길이 길게" 설정이 정상 방향뿐 아니라 반전(↑→↓, ←→→) 임계값까지 함께 스케일. ㅚ/ㅞ 등 멀티 스트로크 모음 인식이 swipe length preset 에 일관되게 비례.

### 권한
- **전체 접근(Full Access) 선택적 사용** — 햅틱 진동을 위해 `RequestsOpenAccess` 활성화. 기본 입력은 권한 없이 동작하며, 키 입력은 외부로 전송되지 않음 (햅틱·사운드·앱 설정 동기화에만 사용).

### 알려진 이슈
- **지구본 키 연속 전환 시 깜빡임 누적 (iOS 시스템 제약)** — 다른 키보드 ↔ 모아+ 를 지구본 키로 *연속해서* 여러 번 전환하면, 전환 순간의 깜빡이는 회색 영역 크기가 점점 커집니다. 키보드는 매번 정상 위치/크기로 복귀하므로 입력은 정상 동작하며, 일반 사용(1~2회 전환)에서는 거의 체감되지 않습니다. iOS 가 키보드 전환 시 시스템 입력 컨테이너 frame 을 직접 누적 조작하는 OS 동작으로, 익스텐션 코드로 해결 불가함이 다각도 검증으로 확인되었습니다 (확인 환경: iOS 26.4, 이전 버전 미검증 — 상세: [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md)). Apple Feedback 제출 예정.

---

## v1.4 (build 7) — 2026-05-09

### 새 기능
- **레이아웃 커스터마이즈** — 설정 → 키보드 → 레이아웃에서 모던 / 클래식 / 확장형 3가지 프리셋 중 선택. 첫 실행 시 모드 선택 모달.
- **슬롯 B 모음 키** — 스페이스 옆 키를 모음 입력 키로 변경 가능. 자음 키 드래그와 동일한 multi-stroke 모음 입력 (ㅑ ㅕ ㅛ ㅠ ㅒ ㅖ ㅢ ㅘ ㅙ ㅚ ㅝ ㅞ ㅟ 모두 입력).
- **A1 백스페이스 ↔ ㆍ swap** — 모던 모드에서 백스페이스 위치를 1.1 위치(아래)로 옮길 수 있는 토글.
- **A2/A3 우측 컬럼 사용자 매핑** — 클래식/확장형 모드의 col 6 키들을 자유 매핑.
- **슬롯 C 사용자 매핑** — col 0 (~^;*) 4 셀 자유 매핑.
- **마지막 모드 기억** — 한글/영문 마지막 사용 모드 유지 옵션 (설정 → 키보드 → 입력 동작).

### UX 개선
- **설정 IA 통일** — 메인 → 키보드 / 외형 / 반응 / 단축어 / 도움말 / 앱 정보 6 항목 평면 구조.
- **새 페이지** — KeyboardSettingsView, BackspaceSettingsView, InputBehaviorSettingsView, LongPressSettingsView, HelpView.
- **제스처 미리보기 위치** — 입력 좌표 반대편에 표시되어 손가락이 가리지 않음.
- **튜토리얼 건너뛰기** + **도움말 진입점**.
- **긋기 테스트 화면 개선** — 실제 키보드로 직접 입력 테스트 + 섹터 캔버스에 입력 결과 표시.

### 레이아웃 정리
- 영문/심볼 모드의 함수행 슬롯 B 키 제거 (스페이스가 흡수).
- 심볼 키패드 백스페이스 위치가 한글 백스페이스와 일치.
- 영문 shift / 백스페이스 1.5x 폭, 좌우 정렬.
- 함수행 토글 버튼 (123/한글) 폭이 좌측 col 0 키와 일치.
- 함수행 엔터 폭이 한글/영문 모두 동일.
- 클래식/확장형의 가로 백스페이스가 위 행과 정확히 정렬.

---

## v1.3 (build 6)

- Caps Lock 롱프레스 (영문 대문자 고정).
- 약어 토글 on/off 설정.
- 메모리 및 동기화 안정성 개선.

## v1.2 (build 5)

- 제스처 기반 모음 입력 (8방향 + 천지인 멀티스트로크).
- 커스텀 테마 / 배경 이미지 / 키 투명도.
- 외형 설정 미리보기에 실제 KeyboardView 렌더링.

## v1.0 – v1.1

- 초기 모아키 제스처 한글 입력 구현.
- 영문 QWERTY, 심볼 키패드.
- 약어 확장 (Trie 기반), 롱프레스 보조 입력.
- 햅틱 / 클릭 사운드, 기본 테마.
